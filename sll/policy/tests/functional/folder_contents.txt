Log in as the portal owner::

    >>> from plone.app.testing import TEST_USER_NAME, TEST_USER_PASSWORD
    >>> browser.login(TEST_USER_NAME, TEST_USER_PASSWORD)

Check that there are no link in portal folder_contents to /folder86/folder_contents::

    >>> browser.open('/folder_contents')
    >>> 'http://nohost/plone/folder86/folder_contents' in browser.contents
    False

Go to batch two and check that there are link to /folder86/folder_contents::

    >>> browser.open('/folder_contents?pagenumber=2&sort_on=getObjPositionInParent')
    >>> 'http://nohost/plone/folder86/folder_contents' in browser.contents
    True
