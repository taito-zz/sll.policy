Initial imports and defines.
----------------------------
    >>> portal_url =  portal.absolute_url()
    >>> folder01_url = portal.folder01.absolute_url()
    >>> folder02_url = portal.folder01.folder02.absolute_url()
    >>> doc01_url = portal.folder01.folder02.doc01.absolute_url()

Setting up.
-----------
    >>> try:
    ...     from Testing.testbrowser import Browser
    ... except ImportError:
    ...     from Products.Five.testbrowser import Browser
    >>> browser = Browser()

For debugging.
--------------
    >>> browser.handleErrors = True
    >>> portal.error_log._ignored_exceptions = ()

Turn off portlets.
------------------
    >>> from zope.component import getUtility, getMultiAdapter
    >>> from plone.portlets.interfaces import IPortletManager
    >>> from plone.portlets.interfaces import IPortletAssignmentMapping

    >>> column = getUtility(IPortletManager, name=u"plone.leftcolumn")
    >>> assignable = getMultiAdapter((portal, column), IPortletAssignmentMapping)
    >>> for name in assignable.keys():
    ...     del assignable[name]

    >>> column = getUtility(IPortletManager, name=u"plone.rightcolumn")
    >>> assignable = getMultiAdapter((portal, column), IPortletAssignmentMapping)
    >>> for name in assignable.keys():
    ...     del assignable[name]

Check that there are not breadcrums.
------------------------------------
    >>> browser.open(portal_url)
    >>> '<a href="http://nohost/plone">SLL</a>' in browser.contents
    False
    >>> '<a href="http://nohost/plone/folder01">Folder01</a>' in browser.contents
    False
    >>> '<span>Folder02</span>' in browser.contents
    False
    >>> '<span>Doc01</span>' in browser.contents
    False


Go to the folder01 with INavigationRoot interface, then there should be the breadcrums.
-------------------------------------------------------------------------------------
    >>> browser.open(folder01_url)
    >>> '<a href="http://nohost/plone">SLL</a>' in browser.contents
    True
    >>> '<a href="http://nohost/plone/folder01">Folder01</a>' in browser.contents
    True
    >>> '<span>Folder02</span>' in browser.contents
    False
    >>> '<a href="http://nohost/plone/folder01/folder02">Folder02</a>' in browser.contents
    False
    >>> '<span>Doc01</span>' in browser.contents
    False


Go to the folder02
------------------
    >>> browser.open(folder02_url)
    >>> '<a href="http://nohost/plone">SLL</a>' in browser.contents
    True
    >>> '<a href="http://nohost/plone/folder01">Folder01</a>' in browser.contents
    True
    >>> '<span>Folder02</span>' in browser.contents
    True
    >>> '<a href="http://nohost/plone/folder01/folder02">Folder02</a>' in browser.contents
    False
    >>> '<span>Doc01</span>' in browser.contents
    False

Go to doc01
-----------
    >>> browser.open(doc01_url)
    >>> '<a href="http://nohost/plone">SLL</a>' in browser.contents
    True
    >>> '<a href="http://nohost/plone/folder01">Folder01</a>' in browser.contents
    True
    >>> '<span>Folder02</span>' in browser.contents
    False
    >>> '<a href="http://nohost/plone/folder01/folder02">Folder02</a>' in browser.contents
    True
    >>> '<span>Doc01</span>' in browser.contents
    True

    >>> open('/tmp/testbrowser.html', 'w').write(browser.contents)
